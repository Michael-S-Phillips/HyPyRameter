from setuptools import find_packages, setup

setup(
    name='hypyrameter',
    packages=find_packages(),
    version='0.1.0',
    description='Calculate spectral parameters on hyperspectral data.',
    author='Michael S. Phillips',
    # license='?',
)
